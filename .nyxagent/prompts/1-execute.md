Récupère le ticket à implémenter dans {{ current.selection.artefacts.selected.md }} tu y trouvera le ticket choisi ainsi qu'un peu de contexte, n'hésite pas a consulter les issues liées (au moins la parente).

Implémente en utilisant /implement

Une fois terminé, ajoute un label `implemented` sur l'issue et produit un petit résumé de ton travail dans {{ current.artefacts.implementation.md }}