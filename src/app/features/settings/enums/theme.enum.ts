/**
 * Public themes available in settings.
 * Add new themes here and create corresponding CSS in src/theme/
 */
export enum Theme {
  Original = 'original',
  Cyberpunk = 'cyberpunk',
  Nexus = 'nexus',
  Pixel = 'pixel',
  Glass = 'glass',
  Konami = 'konami', // Secret theme, only visible after discovery
}
