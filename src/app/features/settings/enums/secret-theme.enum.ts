/**
 * Secret themes unlocked via easter eggs (e.g., Konami code).
 * Add new themes here and create corresponding CSS in src/theme/
 */
export enum SecretTheme {
  None = 'none',
  Konami = 'konami',
  // Future themes can be added here, e.g.:
  // Retro = 'retro',
  // Neon = 'neon',
  // Matrix = 'matrix',
}
